"""Noisy (and optionally biased) measurement model.

Adds Gaussian noise — and optional constant bias — to a chosen measurement map,
drawn from the injected RNG. Drives telemetry-based filter tuning.

Status: stub — implemented alongside the observer/LQG work.
"""
from __future__ import annotations
