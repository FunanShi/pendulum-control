"""viz — visualization rendered from a Telemetry log.

Animation of the links and a dashboard of time-series. Depends on
``telemetry`` (and matplotlib) only — rendering reads a finished log, so
runs stay deterministic and testable independent of plotting.
"""
