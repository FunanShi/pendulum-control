"""model — the plant: *what the system is*.

Physical parameters, the equations of motion, and the linearization about an
equilibrium. Depends on ``util`` only; never imports ``sim``, ``controllers``,
``estimation``, or ``sensors``.
"""
